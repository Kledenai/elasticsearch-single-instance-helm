apiVersion: v1
kind: Service
metadata:
  name: {{ include "opendistro.fullname" . }}
  labels:
    {{- include "opendistro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opendistro.selectorLabels" . | nindent 4 }}
