apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opendistro.fullname" . }}-test-connection"
  labels:
    {{- include "opendistro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opendistro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
